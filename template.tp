{{arr.length}}
<ul>
	{{#each arr, index as entity}}
		<li {{#event type="click" action="edit"}}}>{{index}}: {{entity}}</li>
		{{#log entity}}
	{{#else}}
		<li>liste leer</li>
	{{/each}}
</ul>

{{>sub}}