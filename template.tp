{{variable}}<input alt="alternative text" value={{new}} type="text" class={{classVariable}} name="test">
{{arr.length}}
<ul>
	{{#each arr, index as entity}}
		<li>{{index}}: {{entity}}</li>
		{{#log entity}}
	{{#else}}
		<li>liste leer</li>
	{{/each}}
</ul>

{{>sub}}